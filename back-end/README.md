# Back-end 

---

### Using:
- Maven Apache
- Firebase Java

#### to run:
cd to back-end

```
cd back-end

# only for recompiling the project (back-end springboot)
mvn clean install -U
# only for recompiling the project (back-end springboot)

mvn spring-boot:run
```

run this on terminal only applicable to @tcker